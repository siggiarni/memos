# Tmux memo

## The command prefix

* `Ctrl-b`

## Sessions

Create a session called _fnord_.

* `tmux new -s fnord`

List sessions.

* `tmux ls`

Attach to a session named _session-name_

* `tmux attach -t session-name`
